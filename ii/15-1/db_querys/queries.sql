-- SQL statements for C program queries and inserts
-- Place your SQL queries here, one per line or as blocks, and read them from C code as needed.

-- Example: (replace with your actual queries)
-- SELECT * FROM students;
-- INSERT INTO students (eid, fname, lname, uni_id) VALUES (?, ?, ?, ?);
